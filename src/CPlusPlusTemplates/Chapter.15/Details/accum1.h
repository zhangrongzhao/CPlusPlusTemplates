#include "../stdafx.h"
/*
#ifndef ACCUM_H
#define ACCUM_H

template<typename T>
inline T accum(T const* begin,T const* end){
	T total = T();//假设T()会生成一个等于0的值
	while (begin!=end){
		total += *begin;
		++begin;
	}
	return total;
}

#endif //ACCUM_H

*/