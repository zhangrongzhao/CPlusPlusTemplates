#include "stdafx.h"
#include "funcwrap.h"

//int main(){
//    //创建含有10个元素的vector
//	std::vector<int> v(10);
//
//	//用封装函数（重新）初始化vector的值
//	initialize<FunctionReturningIntWrapper<random_int>>(v);
//
//	//输出vector中的元素的值
//	for (std::vector<int>::size_type i = 0; i < v.size();++i){
//		std::cout << "coll[" << i << "]" << v[i] << std::endl;
//	}
//}

